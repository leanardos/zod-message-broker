{
  "name": "zod-message-broker",
  "version": "1.0.0",
  "description": "Message broker for zod library",
  "main": "index.js",
  "scripts": {
      "build": "tsup src/index.ts --format cjs,esm --dts",
      "lint": "tsc",
      "test-watch": "vitest",
      "test": "vitest run",
      "ci": "pnpm run lint && pnpm run test && pnpm run build"
  },
  "author": "Anar Sadigli",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.14.0",
    "tsup": "^6.6.3",
    "typescript": "4.9.5",
    "vitest": "^0.28.5"
  }
}
